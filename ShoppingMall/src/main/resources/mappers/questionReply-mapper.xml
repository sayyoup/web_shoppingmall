<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.itwill.joo.repository.QuestionReplyRepository">
       <insert id="insertReply">
        insert into QUESTIONS_REPLIES (Q_ID, REPLY_CONTENT, RCREATED_TIME,
        RMODIFIED_TIME)
        values (#{q_id}, #{reply_content}, systimestamp, systimestamp)
    </insert>
    
    </mapper>